# Repository examples load .env from this directory; never commit credentials.
# For an embedded application, place .env beside its settings.yaml instead.
# Profiles reference these names explicitly with $VARIABLE.
MODEL_BASE_URL=http://127.0.0.1:8000/v1
MODEL_ID=incoai/Qwen3.8-27B-Splash
# Optional authenticated provider: set api_key: $MODEL_API_KEY in its profile.
# MODEL_API_KEY=your-api-key
