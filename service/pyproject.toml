[project]
name = "foliqant-service"
version = "0.1.0"
description = "Typed, deterministic workflows around model and MCP operations"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
  "foliqant-decisions==0.1.0",
  "pydantic==2.13.5",
  "pydantic-ai-slim==2.46.0",
  "PyYAML==6.0.3",
  "jsonschema==4.26.0",
  "referencing==0.37.0",
  "python-dotenv==1.2.3",
]

[project.scripts]
foliqant = "foliqant.cli:main"

[project.optional-dependencies]
openai = ["pydantic-ai-slim[openai]==2.46.0"]
anthropic = ["pydantic-ai-slim[anthropic]==2.46.0"]
bedrock = ["pydantic-ai-slim[bedrock]==2.46.0"]
azure = ["pydantic-ai-slim[openai]==2.46.0", "azure-identity==1.25.3"]
mcp = ["mcp==2.2.0", "httpx2==2.13.0", "cryptography==50.0.1"]
http = ["starlette==1.6.0", "uvicorn==0.53.0", "PyJWT[crypto]==2.14.0", "httpx2==2.13.0"]
redis = ["redis==8.1.0"]
postgres = ["psycopg[binary]==3.3.6"]
telemetry = [
  "opentelemetry-api==1.44.0",
  "opentelemetry-sdk==1.44.0",
  "opentelemetry-exporter-otlp-proto-http==1.44.0",
]

[dependency-groups]
dev = [
  "pytest==9.1.1", "pytest-asyncio==1.4.0", "mypy==2.3.1", "ruff==0.16.8",
  "types-PyYAML==6.0.12.20260906",
  "types-jsonschema==4.26.0.20260518",
]

[build-system]
requires = ["hatchling==1.32.4"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/foliqant"]

[tool.uv.sources]
foliqant-decisions = { path = "../packages/decision-contracts" }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_mode = "auto"
addopts = "-ra -m 'not live_model'"
markers = [
  "integration: real local protocol or database integration",
  "live_model: explicitly enabled model inference; never part of default tests",
]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.ruff.lint.isort]
known-first-party = ["foliqant"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src"]
plugins = ["pydantic.mypy"]

[tool.pydantic-mypy]
init_forbid_extra = true
init_typed = true
warn_required_dynamic_aliases = true
