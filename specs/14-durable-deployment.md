# Durable deployment and HTTP intake — superseded

Status: **SUPERSEDED / OUT OF SCOPE** as of 2026-09-21.

The project owner clarified that durable intake, application authentication,
packaged HTTP/Redis transports, storage profiles, migration and worker commands
are outside the workflow-service scope. A small runnable HTTP example may call the in-memory API,
but it owns no package contract. This file is retained only as a historical marker
and provides no implementation or acceptance authority.

The active contract is [specification 11](11-workflow-service.md).
