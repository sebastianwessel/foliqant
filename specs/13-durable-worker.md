# Durable execution worker — superseded

Status: **SUPERSEDED / OUT OF SCOPE** as of 2026-09-21.

The project owner clarified the scope of the workflow service as foreground in-memory
execution. Queue polling, claims, heartbeat leases, checkpoint resume, worker
processes and crash recovery are not active service requirements. This file is
retained only as a historical marker and provides no implementation or acceptance
authority.

The active contract is [specification 11](11-workflow-service.md).
