# Model representation catalog

This document covers model-tooling representations only. Unversioned runtime
workflow, flow, step, result and evaluation representations are registered in
[the combined catalog](representation-catalog.yaml) and
[package generation map](package-generation-map.yaml); specification 11 owns
their behavior. Model-data versions and immutable artifacts below are unchanged.

Version `1`. This catalog maps every public or durable representation in the
model lifecycle to one canonical owner and serialization. Exact field rules are
in `model-contracts.md`; this file does not redefine them.

## Generation and ownership

- Canonical source: typed closed classes registered in
  `model/src/foliqant_model/schemas.py`, from model contracts and curation.
- Generated representations: one Draft 2020-12 JSON Schema per root shape in
  `model/schemas/`, produced only by `foliqant-model schema --output`.
- Drift check: `foliqant-model schema --check model/schemas` compares
  canonical generated bytes and fails with `SCHEMA_DRIFT`.
- JSON/YAML configuration is parsed to the same canonical input classes. YAML is
  only an input syntax; no YAML-specific domain representation exists.
- JSONL records use the named item schema on every line. A JSONL file is not an
  untyped object stream.
- Python internal values may add private computed state, but no second public DTO
  or handwritten schema may mirror these representations.

## Input representations

| Representation | Wire occurrence | Producer | Consumers | Mapping |
|---|---|---|---|---|
| `CategoryCatalog` | caller-authored configuration | application author | native question builder | deterministic key-format normalization; reject collisions; preserve descriptions; convert to existing `DecisionOption` values; no change to historical V1 artifacts |
| `ChatMessage` | source JSONL inside `DataRecord` | data owner | prepare, evaluation prompt builder | identity after NFC/LF normalization for hashes; original validated text is retained |
| `DataRecord` | source JSONL and prepared full-record JSONL | data owner / prepare | prepare, train preflight, evaluation | source object to validated closed record; chat-only training row removes metadata and retains messages |
| `SourceDeclaration` | `DatasetConfig.sources[]` | operator | prepare rights gate | path resolves relative to config; durable mapping is `ResolvedSourceDeclaration` plus top-level `SourceRight` |
| `DatasetConfig` | UTF-8 JSON/YAML <=1 MiB | operator | prepare | defaults materialized to `ResolvedDatasetConfig`; canonical digest persisted |
| `ResolvedSourceDeclaration` | `DatasetDetails.datasetConfig.sources[]` | prepare | verify, training rights gate | `SourceDeclaration` without local path; all other fields preserved |
| `ResolvedDatasetConfig` | `DatasetDetails.datasetConfig` | prepare | verify, docs/evidence | normalized config with resolved source declarations |
| `TrainConfig` | UTF-8 JSON/YAML <=1 MiB and `AdapterDetails` | operator / train/customize | train/customize, verify | defaults materialized; exact canonical object and digest persisted |
| `EvaluationConfig` | UTF-8 JSON/YAML <=1 MiB | operator | evaluate | defaults materialized; one closed same-document-ref output schema is checked and copied byte-for-byte into the evaluation artifact; full config digest persisted |

## Artifact and lineage representations

| Representation | Wire occurrence | Producer | Consumers | Mapping |
|---|---|---|---|---|
| `ArtifactManifest` | artifact `manifest.json` | each artifact-producing command | every downstream command, verify | canonical JSON; `details` selected by `kind`; `artifactId` omitted only while computing identity |
| `ParentRef` | `ArtifactManifest.parents[]`, recursively | artifact finalizer | lineage, integrity, leakage and rights checks | canonical immutable parent manifest snapshot without `schemaVersion`; never a path or mutable alias |
| `FileEntry` | manifest inventory | artifact finalizer | verify | filesystem entry to safe relative path, byte size and SHA-256 |
| `InventoryFileRef` | kind details | artifact finalizer | verify and domain reader | must resolve to the identical `FileEntry`; adds record count and serialization format |
| `ProducerIdentity` | manifest | command boundary | verify, reproducibility evidence | installed package, source identity, Python/platform and relevant component versions |
| `VersionedComponent` | producer/profile/result | command/backend adapter | evaluation/profile and release evidence | one normalized component record per `(role,name)` |
| `SourceRight` | manifest transitive rights list | prepare and lineage union | train/customize/merge/export/verify | path-free mapping of source declaration; collision requires canonical equality |
| `PrecisionChange` | quantized/merged/export details | converter/fusion adapter | verify, release review | one ordered, explicit precision operation; never described as lossless unless `lossy=false` is evidenced |
| `CompatibilityRecord` | model/export details | export/fetch/converter finalizer | release review | status plus nullable/required evidence according to status |
| `CompatibilityEvidence` | compatibility record | real runtime check | verify, release review | hashes the invoked command and captured result; contains no generated content |
| `RuntimeSmokeReport` | caller-chosen external evidence JSON | independent checkpoint or GGUF smoke script | release review | closed target-discriminated report; binds the verified export, runtime, executable, bounded invocation hash and result hash without mutating the artifact |
| `CheckpointRuntimeSmokeResult` | `RuntimeSmokeReport.result` for `transformers` | checkpoint smoke script | release review | bounded CPU generation observation with generated text, token count, elapsed time and runtime versions |
| `GgufRuntimeSmokeResult` | `RuntimeSmokeReport.result` for `llama.cpp` | GGUF smoke script | release review | bounded single-turn process observation with captured output, return code and elapsed time |

## Dataset and leakage representations

| Representation | Wire occurrence | Producer | Consumers | Mapping |
|---|---|---|---|---|
| `SourceFileSummary` | `DatasetDetails.sourceFiles[]` | prepare | verify, reproducibility checks | original local file to source ID, byte hash/size and accepted record count |
| `ResolvedSplitSettings` | `DatasetDetails.splitSettings` | prepare | verify | normalized seed and fractions |
| `RecordAssignment` | `DatasetDetails.assignments` typed map | prepare | train/evaluate/verify | record ID to deterministic split, component and privacy-safe group IDs |
| `OriginCounts` | partition summary | prepare | docs/evidence | exact counts for all three origins |
| `PartitionCounts` | four-value partition map | prepare | CLI result, train/evaluate | exact component, record, language, source and origin counts |
| `DatasetDetails` | dataset manifest details | prepare | train/customize/evaluate/verify | prepared files plus deterministic assignment/index metadata; large rows stay in inventoried JSONL |
| `LeakageEntry` | `leakage.jsonl` | prepare; subset copied by train/customize | train/customize/evaluate/calibrate/audit | privacy-safe exact-match index; no plaintext group key or message |
| `LeakageIndexRef` | dataset/model/adapter details | prepare/train/customize/quantize/merge/export | calibration/test ancestry leakage check, verify | reference to complete sorted JSONL; adapter form unions every submitted train/validation row with model and warm-start exposure, and model descendants copy the union |
| `InheritedLeakageRef` | adapter/quantized/merged/export details | train/customize/quantize/merge/export | standalone verify and exposure-union reconstruction | exact ancestor index copy at `inherited-leakage/<artifactId>.jsonl`; dataset assignments select train/validation rows |

## Model representations

| Representation | Wire occurrence | Producer | Consumers | Mapping |
|---|---|---|---|---|
| `ChatTemplateSource` | model identity | fetch/model preflight | train/evaluate/merge/export/verify | selected template origin and exact path/config pointer/registered default name |
| `ModelIdentity` | checkpoint/quantized/merged/export details and deployment profile | fetch/converter/fusion/export | all model operations | architecture, format, exact raw-config/tokenizer-file-set/template identities and template source |
| `CheckpointDetails` | checkpoint manifest details | fetch | quantize/train/merge/evaluate/verify | pinned upstream identity and safe checkpoint metadata |
| `QuantizedDetails` | quantized manifest details | quantize | train/customize/merge/evaluate/verify | exact parent plus 4/8-bit conversion settings, history and copied exposure index |
| `BackendTrainConfig` | adapter details | train/customize adapter | backend invocation evidence, verify | normalized backend options and artifact/file identities; ephemeral paths excluded |
| `MeasuredBytes` | adapter peak-memory measurement | backend adapter | CLI/docs/evidence | measured integer or explicit unavailable reason; never synthetic zero |
| `AdapterDetails` | adapter manifest details | train/customize | merge/evaluate/verify | parent/dataset/config identity, complete leakage subset and measured outcome |
| `MergedDetails` | merged manifest details | merge | quantize/customize/evaluate/export/verify | fused model identity, both parents, precision history and self-contained exposure union |
| `CheckpointExportMetadata` | checkpoint export branch | export | verify, independent runtime | parsed Safetensors counts |
| `GgufExportMetadata` | GGUF export branch | export | verify, independent runtime | parsed GGUF version/counts/type |
| `ExportDetails` | export manifest details | export | verify, release review | merged parent, format-specific parsed metadata, copied exposure index and immutable unverified compatibility state |

## Evaluation and risk representations

| Representation | Wire occurrence | Producer | Consumers | Mapping |
|---|---|---|---|---|
| `GenerationSettings` | deployment profile | evaluate | backend, profile comparison | fixed temperature plus config seed/token limit |
| `ValidatorSettings` | deployment profile | evaluate | prediction validator, profile comparison | local schema content becomes digest; pointers remain exact strings |
| `ScoringIdentity` | deployment profile | evaluate | profile comparison, audit | fixed named algorithm versions |
| `DeploymentProfile` | evaluation details | evaluate | calibrate/audit | model/runtime/generation/scoring identity; excludes dataset and sample selection |
| `ComponentRepresentative` | evaluation details | evaluate preflight | calibrate/audit | lexicographically smallest record per component, chosen before generation/labels |
| `CountRate` | aggregates and CLI | evaluator/risk evaluator | reports, calibrate/audit | passed/eligible integers plus exact nullable ratio |
| `EvaluationAggregate` | evaluation details | evaluate | CLI, calibrate/audit, reports | fixed metrics plus typed field-pointer map |
| `Prediction` | inventoried private `predictions.jsonl` | evaluate | aggregate, calibrate/audit | raw strings, explicit parsed JSON leaves, separate configured-field presence/equality, validation/scoring results and representative marker |
| `RiskSampleSummary` | policy/audit details and CLI | calibrate/audit | reports | counts only component representatives; eligibility uses applicable validators and finite score |
| `EvaluationDetails` | evaluation manifest details | evaluate | calibrate/audit/verify | private prediction-file reference, selection/representative map, profile and aggregates |
| `PolicyDetails` | policy manifest details | calibrate | audit/verify | immutable representative-level threshold selection or explicit abstain-all |
| `AuditDetails` | audit manifest details | audit | release review/verify | fixed-policy test result at representative level and exact-binomial bound |

`JsonValue` is deliberately open only for `Prediction.expectedJson` and
`Prediction.generatedJson`. Messages and raw predictions remain strings. No
manifest `details`, error data, configuration extension, aggregate, metadata, or
typed map accepts arbitrary JSON.

## Runtime and CLI representations

| Representation | Wire occurrence | Producer | Consumers | Mapping |
|---|---|---|---|---|
| `ConfigIdentity` | run state | command preflight | recovery/inspection | config kind plus canonical resolved-config digest |
| `RunFailure` | terminal run state | command boundary | operator/recovery | reduced durable form of `CliError`; code and redacted message only |
| `RunState` | run workspace JSON | every long-running command | operator/recovery/finalizer | mutable atomic state transitions; never included in finalized artifact inventory |
| `MlxLoraParameters` | worker train namespace and MLX adapter config | train/customize parent | MLX worker, output validator | direct rank/scale/dropout mapping from normalized train config |
| `MlxOptimizerConfig` | worker train namespace and MLX adapter config | train/customize parent | MLX worker, output validator | fixed closed AdamW zero-weight-decay record |
| `MlxTrainNamespace` | train worker request and `adapter_config.json` | train/customize parent and MLX worker | MLX LM, parent output validator | exact snake_case backend namespace; durable path-free projection is `BackendTrainConfig` |
| worker request variants | private request JSON | backend parent adapter | offline child interpreter | closed union for doctor/train/generate/quantize/merge/GGUF; verified local paths only |
| `ValidationObservation` | train worker result | MLX callback | parent, run evidence | callback iteration/loss/time with finite metrics |
| worker result variants | private result JSON | offline child interpreter | backend parent adapter | closed operation-specific result, including typed doctor capability probe; parent inspects referenced files independently |
| `WorkerError` | `WorkerFailure.error` | offline child boundary | backend parent adapter | stable code and redacted message; no arbitrary details |
| `WorkerSuccess` / `WorkerFailure` | private result JSON | offline child boundary | backend parent adapter | request ID and operation must match request; never sufficient alone for finalization |
| `CreatedArtifactResult` | artifact-producing CLI results | command boundary | shell/agent caller | concise path and finalized artifact identity/stage |
| command result variants | `CliSuccess.result` | matching command | shell/agent caller | closed discriminated union keyed by command; no generic payload |
| `CliSuccess` | stdout | CLI boundary | shell/agent caller | one compact JSON line on success |
| `ErrorLocation` | CLI error | validator/command boundary | shell/agent caller | one redacted typed location |
| `CliError` | CLI failure | CLI boundary | shell/agent caller, run-state mapper | stable code/category/message plus nullable location/artifact |
| `CliFailure` | final stderr line | CLI boundary | shell/agent caller | one compact JSON line; exit code must match code table |

## Artifact details dispatch and generated schema names

The manifest schema uses a discriminated `oneOf` with these exact branches:

| `kind` | `details` | Root schema file |
|---|---|---|
| `dataset` | `DatasetDetails` | `artifact-manifest.schema.json` |
| `checkpoint` | `CheckpointDetails` | `artifact-manifest.schema.json` |
| `quantized` | `QuantizedDetails` | `artifact-manifest.schema.json` |
| `adapter` | `AdapterDetails` | `artifact-manifest.schema.json` |
| `merged` | `MergedDetails` | `artifact-manifest.schema.json` |
| `export` | `ExportDetails` | `artifact-manifest.schema.json` |
| `evaluation` | `EvaluationDetails` | `artifact-manifest.schema.json` |
| `policy` | `PolicyDetails` | `artifact-manifest.schema.json` |
| `audit` | `AuditDetails` | `artifact-manifest.schema.json` |

Other generated root schemas are
`dataset-config.schema.json`, `data-record.schema.json`,
`train-config.schema.json`, `evaluation-config.schema.json`,
`prediction.schema.json`, `leakage-entry.schema.json`,
`run-state.schema.json`, `cli-success.schema.json`, and
`cli-failure.schema.json`, plus `worker-request.schema.json` and
`worker-result.schema.json`. Shared records appear under `$defs`; they are not
independent wire roots unless listed above.

## Scope links

- Dataset and leakage mappings: `CAP-DATA`, `CAP-TRAIN`, `CAP-EVAL`.
- Model, parent and precision mappings: `CAP-MODEL`, `CAP-TRAIN`, `CAP-EXPORT`,
  `CAP-VERIFY`.
- Prediction/profile mappings: `CAP-EVAL`.
- Representative policy/audit mappings: `CAP-RISK`.
- CLI and run-state mappings: every model lifecycle capability.
- Frontend, webhook, hosted API, python-package and cross-language service DTOs
  are N/A to this model-tooling contract and remain outside this catalog.

Any new wire shape, arbitrary JSON location, or adapter metadata record requires
a contract revision before implementation. Post-export compatibility evidence is
external release evidence bound to the immutable artifact ID and never promotes
or mutates the export manifest.


## Local setup representations

SetupAsset and SetupProfile define exact downloaded files and their immutable
source revisions. SetupReceipt binds those assets, generated recipes/records and
the three prepared child artifact identities. SetupResult is the setup branch of
CliSuccess and exposes only counts and paths. Exact fields and reuse/failure
semantics are in [local setup](../07-local-setup.md). Canonical implementations
live in contracts/setup.py; profile/receipt schemas are generated with the other
contracts. Neither a receipt nor a file hash certifies commercial clearance.

## Automated curation representations

CurationConfig, CurationPlan, CandidateJob and CandidateOutcome live in
`curation/contracts.py`. Their generated public schemas accompany the standalone
FrozenFamilyAssignment and GenerationProvenance schemas. CurateResult is the
`curate` branch of CliSuccess. SourceBatch keeps exact asset references and
observed conversion counts. EndpointModelIdentity describes runtime metadata;
it is never a verified weights checksum. See
[automated curation](../08-automated-data-curation.md) for split, cache and
publication rules. Published output still uses the canonical dataset artifact.

## Runtime decision representation

`foliqant.contracts.decisions.DecisionOutput` owns runtime schema version 3.
It retains typed answers and answerability, requires a nonblank `reason` of at
most 400 characters and nullable `evidence_strength` (`limited` or `strong`), and
has no explanation/citation arrays. Runtime request units omit evidence; their
non-null subjects must occur in allowed input text. Native v2 input and category
contracts are reused. The runtime output is generated under
`schemas/foliqant/runtime/decision-output.schema.json`; see the
[package generation map](package-generation-map.yaml) and
[package semantics](../11-python-package.md#classification-selection-and-unresolved-policy).
No mapping converts a native model-data output to this runtime response.

## Native decision-data representations

`DecisionDataSettings` is the optional closed `CurationConfig.decisionData`
branch that selects native generation while reusing endpoint and generation
limits. `DecisionInput` is the generated-schema wire root for ordered
`state.sources` and typed choice, multiselect, predicate, ordinal and
request-unit questions. Caller IDs, criteria and allowed source IDs are data,
not model-selected metadata. `DecisionOutput` is the generated-schema wire root
for one typed result per question, question-relative answerability, closed issue
codes, citations and concise explanations. The closed issue set is exactly
`no_supported_answer`, `conflicting_information`, and `multiple_valid_options`;
native input/output roots use schema version 2. Malformed questions fail contract
validation. Explicit offline upgrade is the only legacy native acceptance boundary. It
contains no numeric confidence or calibration target. Every request unit has a
required nullable subject: a non-null subject is a literal substring of its
cited evidence and identifies a discriminating reference; a generic request
noun remains null. Request-unit identity, status, category, subject and relations
belong to the semantic signature; generated descriptions and citation prose do
not. Multiselect option order is likewise non-semantic; its unique selected IDs
are compared as a set.

Native seed publication maps canonical `DecisionInput` to the user message and
canonical `DecisionOutput` to the final assistant message of the existing
`DataRecord`. Family, split, origin, reviewed state, source rights and generation
lineage remain canonical `DataRecord`/dataset fields rather than duplicated
inside the decision objects. `CandidateJob` and `CandidateOutcome` are reused;
native jobs use `decision-training` and `native-decision` discriminants.

The private native coverage report records only run identities, counts, cells,
statuses, source projection exclusions and safe candidate reasons. It is not an
artifact-manifest branch, evaluation result, calibration profile or human
approval record. `CurateResult` remains the CLI success variant. Coverage-gate
failure returns the existing typed CLI failure and points to the retained report.
See [native decision-data generation](../09-native-decision-data.md).

The `upgrade-decision-data` CLI result reports the new dataset/report paths,
parent and child artifact IDs, and record count. Its inventory-bound private
`decision-upgrade-provenance.json` records the operation version, verified parent
manifest/file hashes and per-record before/after hashes and splits. These are
migration evidence, not a new native decision wire shape or model verification.
The verifier checks the original parent, source rights, families and row lineage.
