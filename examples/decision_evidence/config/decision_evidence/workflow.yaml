defaults:
  model: local_qwen
input_schema: input.schema.json
output:
  pointer: /flows/assessment/result
  default: null
flows:
  assessment:
    input:
      message:
        pointer: /payload/message
    transition:
      outcome: completed
    on_unresolved:
      outcome: needs_review
