# Workflows

Generated by `foliqant explain --format mermaid --all`; regenerate it after changing the configuration instead of editing it.

## decision_evidence

Start: `assessment`. Output: `/flows/assessment/result`, else its default.

```mermaid
flowchart TD
  start((start))
  flow_assessment["assessment<br/>assess (decision)"]
  outcome_completed(["completed"])
  outcome_needs_review(["needs_review"])
  start --> flow_assessment
  flow_assessment --> outcome_completed
  flow_assessment -.->|"review"| outcome_needs_review
  linkStyle 2 stroke-dasharray: 6 4
```

Diagnostics: none.

## decision_predicate

Start: `assessment`. Output: `/flows/assessment/result`, else its default.

```mermaid
flowchart TD
  start((start))
  flow_assessment["assessment<br/>assess (decision)"]
  outcome_completed(["completed"])
  outcome_needs_review(["needs_review"])
  start --> flow_assessment
  flow_assessment --> outcome_completed
  flow_assessment -.->|"review"| outcome_needs_review
  linkStyle 2 stroke-dasharray: 6 4
```

Diagnostics: none.
