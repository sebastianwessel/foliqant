# Workflows

Generated by `foliqant explain --format mermaid --all`; regenerate it after changing the configuration instead of editing it.

## prompt_security

Start: `security`. Output: `/flows/security/result`.

```mermaid
flowchart TD
  start((start))
  flow_security["security<br/>assess (decision)<br/>confirm (llm)"]
  outcome_completed(["completed"])
  outcome_needs_review(["needs_review"])
  start --> flow_security
  flow_security --> outcome_completed
  flow_security -.->|"review"| outcome_needs_review
  linkStyle 2 stroke-dasharray: 6 4
```

Diagnostics: none.
