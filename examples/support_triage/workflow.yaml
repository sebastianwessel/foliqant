version: 1
name: support_triage
start: classify
defaults:
  model: local_qwen
input_schema: schemas/input.json
output:
  pointer: /steps/extract/result
  optional: true
  default: {status: needs_review}
