# Workflows

Generated by `foliqant explain --format mermaid --all`; regenerate it after changing the configuration instead of editing it.

## support_triage

Start: `triage`. Output: `/flows/triage/result`, else its default.

```mermaid
flowchart TD
  start((start))
  flow_triage["triage<br/>classify (decision)<br/>extract (llm)"]
  outcome_completed(["completed"])
  outcome_needs_review(["needs_review"])
  start --> flow_triage
  flow_triage --> outcome_completed
  flow_triage -.->|"review"| outcome_needs_review
  linkStyle 2 stroke-dasharray: 6 4
```

Diagnostics: none.
