# Workflows

Generated by `foliqant explain --format mermaid --all`; regenerate it after changing the configuration instead of editing it.

## public_request_lookup

Start: `lookup`. Output: `/flows/lookup/result`, else its default.

```mermaid
flowchart TD
  start((start))
  flow_lookup["lookup<br/>lookup (mcp)"]
  outcome_completed(["completed"])
  outcome_needs_review(["needs_review"])
  start --> flow_lookup
  flow_lookup --> outcome_completed
  flow_lookup -.->|"review"| outcome_needs_review
  linkStyle 2 stroke-dasharray: 6 4
```

Diagnostics:

- info `review_ends_run` in `public_request_lookup/workflow.yaml:7:3` at `flows.lookup.on_unresolved`: Flow `lookup` has no review route: when it stops for review the run ends with needs_review and the host receives the projected result of `lookup`.
