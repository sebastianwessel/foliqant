input_schema: input.schema.json
output:
  pointer: /flows/lookup/result
  default:
    status: needs_review
flows:
  lookup:
    input:
      reference:
        pointer: /payload/reference
    transition:
      outcome: completed
