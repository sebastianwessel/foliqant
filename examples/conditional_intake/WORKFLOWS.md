# Workflows

Generated by `foliqant explain --format mermaid --all`; regenerate it after changing the configuration instead of editing it.

## account_intake

Start: `extract` when `/payload/form/request_type present=true`; otherwise `classify`. Output: an object with `queue`, `account`, `review`.

```mermaid
flowchart TD
  start((start))
  flow_classify["classify<br/>classify (decision)"]
  flow_extract["extract<br/>extract? (llm)<br/>check (handler)<br/>repair? (llm)<br/>recheck? (handler)"]
  flow_lookup["lookup<br/>lookup (mcp)"]
  flow_manual_review["manual_review<br/>open_review (handler)"]
  flow_correct["correct [retry]<br/>correct (llm)"]
  outcome_completed(["completed"])
  outcome_needs_review(["needs_review"])
  start -->|"0: /payload/form/request_type present=true"| flow_extract
  start -->|"1: otherwise"| flow_classify
  flow_classify -->|"billing"| flow_extract
  flow_classify -->|"cancellation"| flow_extract
  flow_classify -->|"default"| flow_manual_review
  flow_classify -.->|"review (default)"| flow_manual_review
  flow_extract -->|"0: /flows/extract/result/status equals valid"| flow_lookup
  flow_extract -->|"1: otherwise"| flow_manual_review
  flow_extract -.->|"review (default)"| flow_manual_review
  flow_lookup -->|"0: /flows/lookup/result/plan not_equals Unknown"| outcome_completed
  flow_lookup -->|"1: otherwise"| flow_manual_review
  flow_lookup -.->|"review (default)"| flow_manual_review
  flow_lookup -.->|"retry, continue when /flows/correct/result/status equals corrected"| flow_correct
  flow_manual_review --> outcome_needs_review
  flow_manual_review -.->|"review"| outcome_needs_review
  flow_lookup -.->|"repeat ≤ 2 until plan not_equals Unknown"| flow_lookup
  linkStyle 5 stroke-dasharray: 6 4
  linkStyle 8 stroke-dasharray: 6 4
  linkStyle 11 stroke-dasharray: 6 4
  linkStyle 12 stroke-dasharray: 1 4
  linkStyle 14 stroke-dasharray: 6 4
  linkStyle 15 stroke-dasharray: 1 4
```

Diagnostics: none.
