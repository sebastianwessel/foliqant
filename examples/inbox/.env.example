# Copy this file to the repository root as .env only if your server needs a key.
# The endpoint and exact served model ID are deployment settings in profiles.json.
# LOCAL_MODEL_API_KEY=replace-with-your-local-server-key
