# Workflows

Generated by `foliqant explain --format mermaid --all`; regenerate it after changing the configuration instead of editing it.

Each flow is a box with its steps in order; the shape and color show the step type. A dashed step marked `?` runs only when its condition, shown on the edge into it, holds. Solid edges are routes, dashed edges review routes and dotted edges calls of the flows grouped under callable flows.

```mermaid
flowchart LR
  subgraph legend__["legend"]
    direction LR
    legend_decision__{{"decision"}}
    legend_llm__(["llm"])
    legend_handler__["handler"]
    legend_mcp__[/"mcp"/]
    legend_collection__[["flow_collection"]]
    legend_conditional__["step?<br/>when …"]
    legend_decision__ ~~~ legend_llm__ ~~~ legend_handler__ ~~~ legend_mcp__ ~~~ legend_collection__ ~~~ legend_conditional__
  end
  classDef decision fill:#fff4e5,stroke:#d68a1d,color:#1f2328
  classDef llm fill:#eef3ff,stroke:#3b6fd6,color:#1f2328
  classDef handler fill:#f2f2f2,stroke:#666666,color:#1f2328
  classDef mcp fill:#e9f8ee,stroke:#2f9e5d,color:#1f2328
  classDef collection fill:#f6ecff,stroke:#8a4fd6,color:#1f2328
  classDef conditional stroke-dasharray: 4 3
  classDef group fill:none,stroke:#b8bec6,stroke-dasharray: 4 3,color:#1f2328
  class legend_decision__ decision
  class legend_llm__ llm
  class legend_handler__,legend_conditional__ handler
  class legend_mcp__ mcp
  class legend_collection__ collection
  class legend_conditional__ conditional
  class legend__ group
```

## agent_reply

Start: `answer`. Output: `/flows/answer/result`, else its default.

```mermaid
flowchart TD
  start__((start))
  subgraph answer["answer"]
    direction TB
    answer__answer(["answer<br/>llm · tools"])
  end
  outcome_completed__(["completed"])
  outcome_needs_review__(["needs_review"])
  start__ --> answer
  answer --> outcome_completed__
  answer -.->|"review"| outcome_needs_review__
  classDef llm fill:#eef3ff,stroke:#3b6fd6,color:#1f2328
  classDef flow fill:#fafbfc,stroke:#9aa1ab,color:#1f2328
  classDef terminal fill:#ffffff,stroke:#57606a,color:#1f2328
  class answer__answer llm
  class answer flow
  class start__,outcome_completed__,outcome_needs_review__ terminal
  linkStyle 2 stroke-dasharray: 6 4
```

Diagnostics: none.

## support_email

Start: `classify`. Output: the first present of `/flows/billing/result`, `/flows/cancellation/result`, else its default.

```mermaid
flowchart TD
  start__((start))
  subgraph classify["classify"]
    direction TB
    classify__classify{{"classify<br/>decision · choice"}}
  end
  subgraph billing["billing"]
    direction TB
    billing__extract(["extract<br/>llm"])
    billing__require_reference["require_reference<br/>handler"]
    billing__lookup[/"lookup<br/>mcp"/]
    billing__draft(["draft<br/>llm"])
    billing__extract --> billing__require_reference
    billing__require_reference --> billing__lookup
    billing__lookup --> billing__draft
  end
  subgraph cancellation["cancellation"]
    direction TB
    cancellation__extract(["extract<br/>llm"])
    cancellation__require_reference["require_reference<br/>handler"]
    cancellation__lookup[/"lookup<br/>mcp"/]
    cancellation__draft(["draft<br/>llm"])
    cancellation__extract --> cancellation__require_reference
    cancellation__require_reference --> cancellation__lookup
    cancellation__lookup --> cancellation__draft
  end
  outcome_completed__(["completed"])
  outcome_needs_review__(["needs_review"])
  start__ --> classify
  classify -->|"billing"| billing
  classify -->|"cancellation"| cancellation
  classify -->|"default"| outcome_needs_review__
  classify -.->|"review (default)"| outcome_needs_review__
  billing --> outcome_completed__
  billing -.->|"review (default)"| outcome_needs_review__
  cancellation --> outcome_completed__
  cancellation -.->|"review (default)"| outcome_needs_review__
  classDef decision fill:#fff4e5,stroke:#d68a1d,color:#1f2328
  classDef llm fill:#eef3ff,stroke:#3b6fd6,color:#1f2328
  classDef handler fill:#f2f2f2,stroke:#666666,color:#1f2328
  classDef mcp fill:#e9f8ee,stroke:#2f9e5d,color:#1f2328
  classDef flow fill:#fafbfc,stroke:#9aa1ab,color:#1f2328
  classDef terminal fill:#ffffff,stroke:#57606a,color:#1f2328
  class classify__classify decision
  class billing__extract,billing__draft,cancellation__extract,cancellation__draft llm
  class billing__require_reference,cancellation__require_reference handler
  class billing__lookup,cancellation__lookup mcp
  class classify,billing,cancellation flow
  class start__,outcome_completed__,outcome_needs_review__ terminal
  linkStyle 10,12,14 stroke-dasharray: 6 4
```

Diagnostics: none.

## support_multi

Start: `assess`. Output: `/flows/finalize/result`, else its default.

```mermaid
flowchart TD
  start__((start))
  subgraph assess["assess"]
    direction TB
    assess__identify{{"identify<br/>decision · request_units"}}
  end
  subgraph plan["plan"]
    direction TB
    plan__plan["plan<br/>handler"]
  end
  subgraph process["process"]
    direction TB
    process__requests[["requests<br/>flow_collection → billing_task, cancellation_task"]]
  end
  subgraph finalize["finalize"]
    direction TB
    finalize__disposition["disposition<br/>handler"]
  end
  subgraph callable__["callable flows"]
    direction TB
    subgraph billing_task["billing_task"]
      direction TB
      billing_task__lookup[/"lookup<br/>mcp"/]
      billing_task__prepare["prepare<br/>handler"]
      billing_task__lookup --> billing_task__prepare
    end
    subgraph cancellation_task["cancellation_task"]
      direction TB
      cancellation_task__lookup[/"lookup<br/>mcp"/]
      cancellation_task__prepare["prepare<br/>handler"]
      cancellation_task__lookup --> cancellation_task__prepare
    end
  end
  outcome_completed__(["completed"])
  outcome_needs_review__(["needs_review"])
  start__ --> assess
  assess --> plan
  assess -.->|"review"| plan
  plan --> process
  plan -.->|"review"| outcome_needs_review__
  process --> finalize
  process -.->|"review"| finalize
  process -.->|"calls"| billing_task
  process -.->|"calls"| cancellation_task
  finalize --> outcome_completed__
  finalize -.->|"review"| outcome_needs_review__
  classDef decision fill:#fff4e5,stroke:#d68a1d,color:#1f2328
  classDef handler fill:#f2f2f2,stroke:#666666,color:#1f2328
  classDef mcp fill:#e9f8ee,stroke:#2f9e5d,color:#1f2328
  classDef collection fill:#f6ecff,stroke:#8a4fd6,color:#1f2328
  classDef flow fill:#fafbfc,stroke:#9aa1ab,color:#1f2328
  classDef group fill:none,stroke:#b8bec6,stroke-dasharray: 4 3,color:#1f2328
  classDef terminal fill:#ffffff,stroke:#57606a,color:#1f2328
  class assess__identify decision
  class plan__plan,finalize__disposition,billing_task__prepare,cancellation_task__prepare handler
  class billing_task__lookup,cancellation_task__lookup mcp
  class process__requests collection
  class assess,plan,process,finalize,billing_task,cancellation_task flow
  class callable__ group
  class start__,outcome_completed__,outcome_needs_review__ terminal
  linkStyle 4,6,8,12 stroke-dasharray: 6 4
  linkStyle 9,10 stroke-dasharray: 1 4
```

Diagnostics: none.
