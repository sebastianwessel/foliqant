# Workflows

Generated by `foliqant explain --format mermaid --all`; regenerate it after changing the configuration instead of editing it.

## agent_reply

Start: `answer`. Output: `/flows/answer/result`, else its default.

```mermaid
flowchart TD
  start((start))
  flow_answer["answer<br/>answer (llm)"]
  outcome_completed(["completed"])
  outcome_needs_review(["needs_review"])
  start --> flow_answer
  flow_answer --> outcome_completed
  flow_answer -.->|"review"| outcome_needs_review
  linkStyle 2 stroke-dasharray: 6 4
```

Diagnostics: none.

## support_email

Start: `classify`. Output: the first present of `/flows/billing/result`, `/flows/cancellation/result`, else its default.

```mermaid
flowchart TD
  start((start))
  flow_classify["classify<br/>classify (decision)"]
  flow_billing["billing<br/>extract (llm)<br/>require_reference (handler)<br/>lookup (mcp)<br/>draft (llm)"]
  flow_cancellation["cancellation<br/>extract (llm)<br/>require_reference (handler)<br/>lookup (mcp)<br/>draft (llm)"]
  outcome_completed(["completed"])
  outcome_needs_review(["needs_review"])
  start --> flow_classify
  flow_classify -->|"billing"| flow_billing
  flow_classify -->|"cancellation"| flow_cancellation
  flow_classify -->|"default"| outcome_needs_review
  flow_classify -.->|"review (default)"| outcome_needs_review
  flow_billing --> outcome_completed
  flow_billing -.->|"review (default)"| outcome_needs_review
  flow_cancellation --> outcome_completed
  flow_cancellation -.->|"review (default)"| outcome_needs_review
  linkStyle 4 stroke-dasharray: 6 4
  linkStyle 6 stroke-dasharray: 6 4
  linkStyle 8 stroke-dasharray: 6 4
```

Diagnostics: none.

## support_multi

Start: `assess`. Output: `/flows/finalize/result`, else its default.

```mermaid
flowchart TD
  start((start))
  flow_assess["assess<br/>identify (decision)"]
  flow_plan["plan<br/>plan (handler)"]
  flow_process["process<br/>requests (flow_collection)"]
  flow_finalize["finalize<br/>disposition (handler)"]
  flow_billing_task["billing_task [callable]<br/>lookup (mcp)<br/>prepare (handler)"]
  flow_cancellation_task["cancellation_task [callable]<br/>lookup (mcp)<br/>prepare (handler)"]
  outcome_completed(["completed"])
  outcome_needs_review(["needs_review"])
  start --> flow_assess
  flow_assess --> flow_plan
  flow_assess -.->|"review"| flow_plan
  flow_plan --> flow_process
  flow_plan -.->|"review"| outcome_needs_review
  flow_process --> flow_finalize
  flow_process -.->|"review"| flow_finalize
  flow_process -.->|"requests"| flow_billing_task
  flow_process -.->|"requests"| flow_cancellation_task
  flow_finalize --> outcome_completed
  flow_finalize -.->|"review"| outcome_needs_review
  linkStyle 2 stroke-dasharray: 6 4
  linkStyle 4 stroke-dasharray: 6 4
  linkStyle 6 stroke-dasharray: 6 4
  linkStyle 7 stroke-dasharray: 1 4
  linkStyle 8 stroke-dasharray: 1 4
  linkStyle 10 stroke-dasharray: 6 4
```

Diagnostics: none.
